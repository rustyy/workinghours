import { FlexContainerDirective } from './flex-container.directive';

describe('FlexContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexContainerDirective();
    expect(directive).toBeTruthy();
  });
});
